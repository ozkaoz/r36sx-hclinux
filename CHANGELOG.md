# CHANGELOG.md

Formato: una línea por iteración; detalle técnico en `docs/experiments/` y commits.

## 2026-09-25

- **Iteración 9-6c-FIX (module loader: causa raíz CAZADA — bug gcc magic-div + fix 0005, kernel #44 BUILD PASS):** sondas discriminantes (`hello.ko` 0-UND carga rc=0; `sha256_generic.ko` OOPS determinista ×2 boots) → decode addr2line → cadena exacta `resolve_symbol:1411 → find_symbol:527 → find_exported_symbol_in_section:498 → bsearch → cmp_name:488`. **Causa raíz: gcc Codescape 6.3.0 (mips32) miscompila TODA división constante /12** — emite `sra>>2 + mul` con magic `0xAAAAAAAB` tomando la MITAD BAJA del producto (sin `mfhi`); correcta solo para dividendos múltiplos de 12. Los **hcdrivers vendor inyectan ksymtab entries legacy de 8 B** (sin namespace: `get_adc_default_val_for_check`, `rtw_radiotap_fixup`) → `__ksymtab` 0xa760 = 3570×12+8 → nmemb = **0x55556348 = $6 exacto de ambos OOPS** (verificación matemática cerrada) → bsearch fuera del array → pivot kuseg 0x005ae23c → TLB miss. Fix `0005` (→9005): restar punteros como ENTEROS + `divu` tras asm-barrier (v1 variable y v2 barrier solas fallaron por const-propagation/ptrdiff). Disasm verificado: magic ausente, 2×divu. **Kernel #44 `e9d95de9` BUILD PASS**, gates TOOLCHAIN+PATCH PASS (audit actualizado a 5 genéricos/6 versionados). Descartes: ksymtab sano en archivo/payload/RAM, PHYS_OFFSET=0, ningún patch vendor toca module.c. Docs: `docs/experiments/2026-09-25_9-6c-module-loader-fix.md`. **DEPLOY+TEST FÍSICO PENDIENTE (GO clase F).**
- **Iteración 9-6e-CLOSE (USB networking PRODUCTION PHYSICAL PASS ✅ — 9-6e DONE):** test de producción del usuario con el stack NCM (`66a3cbe`) desplegado en SD: NETWORK → adaptador de red NCM en Windows + `telnet 192.168.137.2` → root bajo el overlay azul (limitación aceptada, ADR-015); salida limpia con B + `restore done rc=0` (evidencia `NET_MODE_DEBUG.log`). **9-6e cerrado**: NCM = transporte de red de producción; overlay azul = limitación documentada (eliminación = firmware AVP propio, aparcado clase D).

## 2026-09-24

- **Iteración 9-6e-PPP (estrategia serial REFUTADA → NCM producción, ADR-015):** build #43 kernel `e07844bd` (8.271.323 B; PPP/PPP_ASYNC/PPP_DEFLATE/SLIP=y + pppd/chat en rootfs; backup `.preppp.bak` en SD) + `net_ppp.sh` (fork). Test físico: gadget ACM + `/dev/ttyGS0` + pppd OK, LCP ×10 **sin respuesta del PC** — **y el overlay azul apareció igual → REFUTADO**: el disparador es **networking activo en general** (gadget CDC-network o pppd sobre ACM), no el netdev del gadget. **Decisión (ADR-015): NCM = transporte de producción** (adaptador nativo Windows + telnet root; azul = limitación display-only aceptada); eliminar el azul solo vía firmware AVP propio (clase D, aparcado). Dispatcher del fork default → NCM (`net-mode-app` `66a3cbe`). Docs: `docs/experiments/2026-09-24_9-6e-ppp-slip.md`.
- **Iteración 9-6e v15-v16 (USB networking / overlay AVP):** la pantalla azul al activar un gadget CDC-network es **comportamiento del AVP**, no corrupción del kernel (fb con relleno uniforme `06 f2`; `CONFIG_MUSB_DMA_XFER_ALIGN` no activo → el 9106 está en dead code; 9105 sin efecto). CDC-ACM (modem) **no** dispara el azul; NCM/RNDIS sí. **v15** (`2fe467e`): fix por DTS — `make_board_dts.sh` deja `usb0 status="disabled"` en `/hcrtos/` (el AVP monitorea el controller 0 vía su nodo); DTB `116ddf26` desplegado, gate NOR-DTB PASS. **v16:** estrategia CDC-ECM (subclase 06, misma familia que ACM pero con netdev); fragment + `net_mode.sh` default ECM. Kernel ECM `7d87d15c` **compilado, NO desplegado**. **TEST FÍSICO v15+NCM (2026-09-24): REFUTADO** — NCM da red + shell ~30 s y luego overlay azul (stateful: persiste como filtro tras B); el disparador es el network gadget (netdev/u_ether), no la subclase ni el nodo DTS `usb0` *(corregido por la iteración 9-6e-PPP de arriba: es networking activo en general)*. Docs: `docs/experiments/2026-09-24_usb-networking-blue-overlay.md`; stack canonizado en el fork TreeFrogUI `apps/net_mode/` (branch `net-mode-app`, `d9ef355`).

## 2026-09-23

- **Iteración 9-6b (USB Mode MTP PHYSICAL PASS ✅):** Windows detecta "TreeFrogUI MTP" + transferencia verificada físicamente en kernel 5.12.4. Fixes: DUAL_ROLE, S90configfs, módulos visibles vía bind, kmod standalone, gadget stack **built-in** (workaround al OOPS del module loader), y el bug raíz del reboot: `config_group_init_type_name` ANTES de `usb_os_desc_prepare_interf_dir` en el port 9103 de f_mtp (list-API 5.12 vs array-API 4.4). Kernel `fdd1d7cc`.
- **Iteración 9-6e (shell remoto + red):** NCM = Windows detecta adaptador de RED (no COM7) + telnet root bajo la pantalla azul; `passwd`/`shadow` en `rootfs/etc`; 5 ports kernel versionados (9101-9105); app `net_mode` upstreamable (AGENTS §15). Overlay azul documentado como pendiente.
- **Backup known-good:** `D:/R36SX/sd-full-backups/2026-09-23_mtp-knowngood/` (MTP PASS ×2, hash-verified).

## 2026-09-22

- **Fase 9-4 COMPLETE — kernel 5.12.4 CLEAN PHYSICAL PASS ✅:** SD purgada de kernel-4 (preservado hash-verified en D:) y la consola arranca SOLO con 5.12.4: menú TreeFrogUI + audio (ADR-012 validado en 5.12) + navegación (port 9101 timer + HC_INPUT) + entrada/salida de emuladores. Boot ~10s. cubegm = contrato NOR de 4 archivos. uImage `9731d6a5`.
- **Fase 9-6a (host USB):** port 9102 (timeval→ktime) + port 9103/9104 (f_mtp/f_ptp/AOA/audio_source desde el patch vendor 0024) + DUAL_ROLE + S90configfs. USB HOST: PHYSICAL PASS (stick OK).
- **Fase 9-6c (module loader roto):** el kernel 5.12 OOPSEA en `resolve_symbol` (module.c:1411) con CUALQUIER `.ko` — el loader muere con SIGSEGV y el mutex queda lockeado. Workaround: gadget functions built-in. Fix real pendiente (bloquea wifi-modules).
- **Iteración 9-1/9-2/9-3:** kernel 5.12.4 BUILD PASS desde el SDK (defconfig k512 + fragment versionado); patch set propio canonizado en `patches/buildroot/linux/` (900X) — ADR-014 (reproducibilidad).

## 2026-09-21

- **CONSOLA REVIVIDA ✅** — La R36SX V2.6 fue restaurada exitosamente mediante el modo chip-en-blanco del BootROM (corto de pines 2/4 del NOR SPI) + HCProgrammer USB. El archivo que funcionó fue `HCFOTA-factory-restore.bin` (988.648 B, generado por HCFota_Generator con el DDR-init de fábrica + hc16xx_jtag_updater.bin + las 3 particiones del NOR con bytes del dump). El NOR quedó con los bytes exactos de fábrica. TreeFrogUI bootea correctamente. Hallazgos: HCProgrammer requiere ambos .exe abiertos simultáneamente; el formato de firmware correcto es HCFOTA.bin (no spinorflash.bin crudo); el modo USB del BootROM persiste mientras el corto 2/4 esté activo. Docs: postmortem (addendum recuperación exitosa).

## 2026-09-20

- **POST-MORTEM del brickeo (docs/experiments/2026-09-20_postmortem-brickeo-bootloader.md):** documento completo con cronología, causa raíz (NOR-DTB compilado desde el DTS stock-normalized de la SD ≠ NOR-DTB de fábrica: panel-init-sequence CORTA vs LARGA + pinmux/uart/LP-CLK-DIV → LCD mal inicializado → bootloader muere antes de cargar la SD → sin Linux → sin recovery), el diff de 89 líneas, el análisis de por qué cada intento de recuperación falló (S07norflash, HCFOTA, HCProgrammer USB, tecla, serial), las 4 lecciones (gate semántico contra la referencia equivocada, evidencia disponible no usada, violación de una-variable-por-boot, flash impecable pero contenido erróneo), el fix-forward diseñado (bl DTS desde factory-nordtb-0.dtb + SELECT upgrade key + gate NOR-DTB-vs-fábrica) y el estado físico de la consola (recuperación requiere CH341A+clip o corto de pines 2/4).

## 2026-09-19

- **Iteración D-2b-RESULT (Fase D — BOOTLOADER PROPIO BUILD PASS + VALIDACIÓN 9A COMPLETA):** build de nuestro hcboot LOGRADO. Piezas: `boards/r36sx-v26/bootloader/r36sx-v26_bl_defconfig` (derivado de `cb_d3100_v10_projector_c3_q6_bl`: CROSS→nuestro toolchains/mips-mti-elf, DTS→r36sx-v26, módulo UPGRADE completo incl. SD — HCFOTA oficial disponible tras el primer flash); **parche dual-path** (`patches/bootloader/0001`: fallback a cubegm/ si el path-prefix falla — bootloader inbrickeable); DTS path-prefix→"boot" vía el GENERADOR (make_board_dts.sh — persistente) + allowlist del gate semántico; wiring BR2_TARGET_HCBOOT + DDR-init de fábrica en el defconfig + sync en build_kernel.sh (sha verificado d944d9af). **Bugs cazados**: make_board_dts pipefail (diff|head abortaba al diferir — mi delta expuso el bug latente), y el DTB embebido quedó "cubegm" por compilarse en el build previo al sync → hcboot-dirclean + rebuild lo corrigió. **VALIDACIÓN 9a**: DDR-init == fábrica byte-exacto salvo el campo de tamaño en 0x20 (413.216 == hcboot.bin — parche dinámico del post-build, correcto por diseño); estructura bootloader.bin = DDR-init+hcboot.bin exacta; **NOR-DTB embebido: path-prefix="boot"** ✓ + bootmem reg 0x9da0000 == stock ✓; dual-path compilado (string `/media/%s/cubegm/%s` presente) ✓; upgrade SD presente ✓; 2.702 strings comunes con fábrica; 425.504 B < partición 0x6C000 ✓. **Imagen de flash**: staging `bootloader-r36sx-v26-faseD2b.bin` (sha `1734c340…`, 442.368 B con padding 0xFF) desplegada a SD + `/boot/` creado con COPIAS IDÉNTICAS de los 4 archivos que ya funcionan (cero variables nuevas) + `tools/d2c_flash_bootloader.sh` (triple verificación: sha imagen + /boot/ completo + cubegm/ fallback intacto → mtdnor erase+write+readback). cubegm/ INTACTO como fallback del dual-path. **PENDIENTE: GO explícito del usuario para el flash (D-2c)** — plan de boots: solo el bootloader cambia (boot-1); después swap de dtb/kernel nuevos en /boot/ (boot-2); D-3 borrar cubegm/ (boot-3).

- **Iteración D-2a-RESEARCH + PIVOTE (Fase D — mecanismo de primera escritura identificado):** investigación completa del formato HCFOTA (header/payload/entries + filename `HCFOTA.bin` en raíz + trigger sysdata.ota_detect_modes). **Bootloader de fábrica DESCOMPRIMIDO del dump** (LZMA @0x5e48, tamaño exacto 0x10cebc): build "hcboot-custom" del proyecto factory `e3100_cube` (origen del nombre cubegm/) — **NOR-DTB de fábrica EXTRAÍDO y decompilado** (external_files/path-prefix="cubegm" verificado en el BINARIO REAL). **HALLAZGO DECISIVO: el bootloader de fábrica NO tiene módulo de upgrade compilado (0 strings de hcfota/upgrade)** → HCFOTA imposible como primera escritura → **PIVOTE a escritura MTD directa desde nuestro Linux** (/dev/mtd0-3 vivos, kernel corre desde RAM, NOR despejado en runtime). Escalera 9a redirigida: **D-2a'** re-escritura de los bytes EXACTOS de fábrica vía MTD con verify (prueba de mecanismo, contenido idéntico, cero riesgo) → **D-2b** nuestro hcboot con **fallback dual-path /boot/→cubegm/ (inbrickeable por diseño)** → **D-2c** flash propio → **D-3** borrar cubegm/. Herramientas construidas: `tools/mtdnor` (MIPS32r2 estático 610 KB: MEMERASE+write+readback-memcmp) + `tools/d2a_flash_factory.sh` (verifica sha256 de la imagen + verify-only del contenido actual ANTES de escribir; sha 9fc95d7e integrado). Despliegue a SD pendiente (SD no montada al cierre). Docs: experimento cubegm (addendum 3).

- **Iteración D-1-RESULT (Fase D — dump NOR ejecutado + análisis completo):** usuario ejecutó `nor-dump.sh` en la consola → dump bit-a-bit verificado (hashes OK, copia en `D:\R36SX\nor-dump-20260919\`). **Layout NOR mapeado desde el dump**: mtd0 "nor" 512K (ventana) = mtd1 "boot" 0x0-0x6C000 (DDR-init 12.288 B + bootloader COMPRIMIDO, DTB embebido en el payload) + mtd2 "eromfs" 0x6C000 (romfs rescate) + mtd3 "persistentmem" 0x70000. **DDR-init de fábrica extraído byte-exacto** (`d944d9af…`) — NO coincide con ningún ddrinit del SDK Jul-2024 → D-2 usará el extraído (jamás uno del SDK). **Mecánica HCFOTA completa** (hcfota/main.c + upgrade.c): `hcfota reboot <modo>` soporta **sd** (hcboot lee HCFOTA.bin de la SD — nuestro medio ideal) + usbhost/usbdevice/network; empaquetado vía `HCFota_Generator --dtb --ini -o for-upgrade[-withboot]` (withboot = incluye bootloader = nuestra variante); el DTB de todo el flujo sale de nuestro DTS. **Estrategia en escalera (método 9a)**: D-2a re-flash del bootloader de FÁBRICA exacto (prueba del mecanismo sin riesgo funcional) → D-2b build de nuestro hcboot (path-prefix="boot") + flash → cubegm/ 100% eliminado. Docs: experimento cubegm (addendum 2).
- **Iteración 8f-RESULT (cubegm al mínimo — PHYSICAL PASS ✅ + FASE D lanzada por decisión del usuario):** boot PASS del usuario con cubegm = contrato NOR de 4 → **confirma que el culpable del fail de la instalación limpia era `xgame-logo.bmp`** (part4 de `external_files` del bootloader); setting.xml/allfiles.lst/root.dat eran solo-fábrica-menú, eliminados sin efecto. cubegm/ = mínimo definitivo sin NOR: {dtb.bin, avp.uImage, vmlinux.uImage, xgame-logo.bmp} + goldens + flag. **Decisión del usuario: intentar la eliminación 100%** → Fase D (bootloader propio): evidencia habilitante — **NOR accesible desde nuestro Linux** (CONFIG_MTD+MTD_M25P80, DTS spi@1882e000, /dev/mtd0..3 vivos en diag6x) → `scripts/nor_dump.sh` desplegado a `G:\nor-dump.sh` (`77678eb0`) para dump bit-a-bit (rollback de fábrica + DDR-init exacto + NOR-DTB real); **HCFOTA = mecanismo oficial de reflash** (manual §16.17: flag persistentmem → reboot → hcboot lee hcfota.bin de USB y se auto-actualiza; nor-only soportado; `hcfota <file-path>` también). Plan: D-1 dump (sin riesgo) → D-2 build hcboot con path-prefix="boot" + DDR-init byte-exacto → D-3 flash HCFOTA (solo con recuperación BootROM/USB verificada + GO explícito; PROHIBIDO flashear sin red completa). Docs: experimento cubegm (addendum 1).

- **Iteración 8f-cubegm-minimal (investigación pre-Linux + contrato NOR):** objetivo del usuario: eliminar `cubegm/` al 100%. **Investigación con evidencia dura**: el path `cubegm/` NO está hardcodeado — es `path-prefix` del nodo `/hcrtos/external_files` del DTB que el bootloader de NOR lee (main.c `get_block_devpath` → `fdtp` global = NOR-DTB hornero de fábrica; el dtb.bin de la SD se pasa a AVP/kernel pero NO re-polariza las búsquedas). El bootloader carga EXACTAMENTE 4 archivos de cubegm: `dtb.bin`, `avp.uImage`, `vmlinux.uImage`, `xgame-logo.bmp` (part4=logo — casi seguro EL culpable del fail de la instalación limpia). El AVP de fábrica NO hardcodea cubegm (strings 0); `setting.xml`/`allfiles.lst`/`root.dat` eran del menú de fábrica (rkgame — fuera del boot propio desde 8d). **Veredicto: 100%-eliminación exige reemplazar el bootloader de NOR (zona prohibida §5, brick risk, no recomendado); mínimo sin tocar NOR = 4 archivos (contrato de boot, análogo al DDR-init).** Ejecutado: eliminados los 3 solo-fábrica-menú → cubegm = 4 boot + goldens + flag. **PHYSICAL TEST pendiente.** Docs: `docs/experiments/2026-09-19_cubegm-minimal-boot-contract.md`.

## 2026-09-18

- **Iteración 8e-BISECT-CLOSE (Fase 8 — CLEAN-INSTALL PHYSICAL PASS ✅ DEFINITIVO — causa raíz aislada por bisect):** secuencia bisect UNA VARIABLE POR BOOT (lección del fail corregida): **(a) kernel 8e** (S99app v2 + S09trace v5.1 + budget snd_xfer) sobre layout cubegm → **PASS, absuelto**; **(b) mudanza treefrog/ + alias bind** → **PASS, absuelto** (evidencia: picoarch ON con stack en treefrog); **(c) eliminaciones COMPLETAS menos los 4 sospechosos pre-Linux** → **PASS — LA INSTALACIÓN LIMPIA FUNCIONA**. **Conclusión: el fail de la primera instalación limpia lo causó la eliminación de uno de los 4 archivos que la cadena pre-Linux (AVP/bootloader) requiere: `setting.xml`, `xgame-logo.bmp`, `allfiles.lst`, `root.dat`** (identificación del exacto = refinamiento opcional; 1,5 MB inertes que se conservan por diseño). **Clean install DEFINITIVA**: `cubegm/` = 10 archivos (boot 5 + 4 pre-Linux-required + flag diag), `treefrog/` stack, `rootfs/` runtime, roms/frogui/picoarch datos — SIN menú de fábrica, SIN icube/rkgame/assets. Evidencia: evidence-8e-cleaninstall-*. Timeline 7b extraído del boot: kernel@1,08s → SD+zhijack@3,4s → picoarch@~7s (floor = init frogui, lado upstream). Nota: con el flag activo S09trace v5.1 demostró el opt-in funcionando (logs con flag, silencio sin flag — ADR-013 físicamente validado en ambos sentidos).

- **Iteración 8e-CLEAN-TEST-FAIL + RECUPERACIÓN (post-mortem + estado known-good restaurado):** el test físico de la instalación limpia FALLÓ: "please insert TF Card", sin splash TreeFrogUI, sin logs aunque el usuario creó el flag de diagnóstico (que quedó como `diag.enabled.txt` — extensión añadida por Windows, nunca activó el opt-in — lección documentada: crear con extensiones visibles o `echo. > diag.enabled`). **Análisis de causa (sin concluir aún — bisect pendiente):** el mensaje = pantalla persistente del AVP cuando Linux/userspace no toma fb0. Se detectó el ERROR DE MÉTODO: entre el último PASS físico (9m) y el test se acumularon TRES deltas jamás probados físicamente: (1) kernel 7a/8e (budget snd_xfer + S99app v2 + S09trace v5 — NUNCA bootados; la "prueba de fase 7" del usuario fue con 9m en SD), (2) la mudanza treefrog/ (nunca bootada), (3) la limpieza de archivos. El formato FAT32 (offset 1 MiB, 32 KB clusters) es descartado por el usuario (mismo offset que fábrica). **RECUPERACIÓN EJECUTADA:** restauración completa del tar verificado + stack devuelto a `cubegm/` (layout pre-mudanza) + **kernel 9m PROBADO** (`1b095ac8`) desplegado. Estado = exactamente el del último PHYSICAL PASS, sobre el FAT32 nuevo (lo que además valida el formato si bootea). **Próximo: boot del usuario (recovery test); después bisect de los 3 deltas UNO POR BOOT: (a) kernel 8e sobre layout cubegm (S99app v2 es backward-compatible), (b) mudanza treefrog con 8e, (c) limpieza.** La instalación limpia queda re-planificada como fase posterior con esa secuencia.
- **Iteración 8e-CLEAN-INSTALL (Fase 8 — instalación limpia 100% propia, formateo incluido):** diseño de prueba del usuario: formatear la SD y reinstalar NUESTRO desarrollo desde cero. **Backup PRE-formato completo y verificado**: `D:\R36SX\sd-clean-install-backup-20260918\sd-full.tar` 1.77 GB sha256 `963dfd23…` (4.742 archivos) + prueba de restauración 5/5 OK + hashes críticos. SD formateada por el usuario (FAT32, R36SX). **Restauración selectiva + poda "100% nuestro sistema"** (decisión del usuario): `cubegm/` reducido a SOLO 5 archivos de boot (kernel 8e `f8fb6768`, dtb fábrica `1258f1eb` —semánticamente == nuestro build—, avp fábrica `a9788995`, goldens `53b3e0b3`/factory.bak); **eliminados 98 ítems de fábrica/cruft** (icube/rkgame/MyExecutable/setting.xml/assets .cpd/.wav/.ttf de cubegm; 14 dirs de sistema placeholder ATARI…WSC — juegos reales viven en `roms/` 621 MB, conservado; MD/ dummy del hijack stock; media dirs vacíos; chkdsk; logs de diagnóstico) — manifiesto de eliminación en `docs/experiments/manifest-8e-cleaninstall-20260918.txt`. **Verificación post-instalación: TODA OK** (kernel/dtb/avp/goldens/zhijack/picoarch hashes ✓; treefrog 2.304 == esperado; rootfs 602/roms 400/frogui 1.280). Layout final raíz: cubegm(5) + treefrog + rootfs + roms + frogui + picoarch + docs + diag*.sh + configs. **PHYSICAL TEST PENDIENTE (usuario): boot → menú TreeFrogUI → juego con audio → video → salida.** Con PASS: CLEAN-INSTALL PHYSICAL PASS cierra 8e definitivamente.
- **Iteración 7a-RESULT (Fase 7 — deploy 7a a SD + revisión de pendientes + CONTRATO FORMAL + PLAN 8E):** SD vuelta al lector (verificada/montada; kernel era 9m, goldens intactos). **Deploy 7a `2546d199` verificado read-back** (producción: diagnósticos opt-in ADR-013). **Evidencia del test 9m cosechada** (evidence-9m-*: log.prev contiene la sesión VIDEO_PLAYER completa del test exitoso — 52 líneas; boottrace/virtuart del boot post-test). **Revisión de pendientes de fases anteriores → ejecutados:** (1) **`docs/TREEFROG_UI_CONTRACT.md`** — el entregable formal de Fase 6 que faltaba: boot flow verificado, servicios, ABI /dev viva, contrato ABI media (ADR-012), inventario cubegm/ clasificado con hashes, garantías para rootfs futuros; (2) **ADR-011 → SUPERSEDED** (boot parcial resuelto sin serial; DTB diagnóstico queda como herramienta); (3) **plan 8e APROBADO Y DOCUMENTADO** (`docs/experiments/2026-09-18_8e-stack-relocation-plan.md`): relocación del stack TreeFrogUI fuera de `cubegm/` en 4 fases (8e-1 fork TreeFrogUI parametriza TF_INSTALL_DIR → 8e-2 S99app v2 en este repo → 8e-3 `mv` en SD con backup de inventario → 8e-4 deploy+test; rollback stock queda intacto en cubegm, pierde solo el hijack — menú fábrica usable). CONTEXT_MAP actualizado con las rutas nuevas. **Pendiente 8e-1: trabajo en el repo fork TreeFrogUI.**
- **Iteración 9m-RESULT (Fase 8/media — PHYSICAL PASS ✅ TOTAL: video + audio + salida de cores — FASE 8 COMPLETA):** test físico del usuario con el 9m (`1b095ac8`): **los VIDEOS muestran imagen y suenan correctamente; audio OK en juegos y música; la salida de los emuladores vuelve al menú sin colgarse** — el checklist de 8g queda íntegro (audio/música/video/salida-cores = UNA causa raíz: drift ABI, ADR-012; confirmada por los fixes gemelos 9l auddec +24 B / 9m vidmp +20 B). **Funcionamiento declarado CORRECTO bajo nuestro kernel.** Evidencia on-device (boottrace/log.txt del test) autocapturada en la SD — cosecha al volver la SD al lector (pase pendiente). ROADMAP: Fase 8 ✅ DONE.
- **Iteración 7a (Fase 7 — OPTIMIZACIÓN 1: diagnóstico opt-in/self-limiting):** S09trace v5: sin flag `cubegm/diag.enabled` **NO escribe NADA a la SD** (v4 hacía ~30 ciclos copy+sync de 3 logs durante 60s en CADA boot ≈ 5+ MB/boot de escritura + churn de I/O en el primer minuto de juego — desgaste SD); buffers solo en /tmp (RAM) para inspección en shell vivo y auto-apagado tras el probe post-picoarch; con flag = comportamiento v4 completo (docs: crear `G:\cubegm\diag.enabled` y reboot para debuggear). avp-proxy: `snd_xfer_dbg` budget 500 entradas (el print por-cada-xfer-PCM de 9g se auto-limita — mismo patrón que amprpc_dbg). ADR-013. BUILD PASS: uImage fase7a **`2546d199` 7,128,430 B** (Entry 0x803db980), S09trace v5 verificado embebido (cpio sha == repo `d22bf68a`), ABI fixes intactos (probe: video 664/0x82980400, audio 632/0x82780301). STAGED — deploy pendiente de SD en lector (la SD quedó en la consola tras el test).

- **Iteraciones 9i–9l (Fase 8/media — CAUSA RAÍZ AUDIO: drift ABI 24 bytes → FIX → AUDIO PHYSICAL PASS ✅):** builds 9i (`7127957`) / 9j (`7127751`) / 9jfix (`7128161`) / 9k (`7127849`) con instrumentación amprpc refinada (9i/9j: filtros anti-spam cubevol sndC0i2so/backlight; log OPEN/IOCTL con ret) + S09trace v4 (dmesg post-juego). Evidencia on-device decisiva (boottrace 9l): el driver_r36sx.so de fábrica (Dic-2025) envía `AUDDEC_INIT=0x82780301` (sizeof **632**) pero el case del avp-proxy compilado con headers del SDK Jul-2024 espera `0x82600301` (sizeof **608**) → **nunca matchea → `KSHM_WRITE_HDL_ACCESS` jamás se envía al AVP → audio muerto** (el "aborto silencioso" de 9h: la RPC AUDDEC_INIT se reenvía por defecto — el AVP responde buffer_size/volume — pero sin el handle kshm el userspace hace deinit). **FIX 9l: `_pad_abi_2025[24]` al final de `struct audio_config`** (`include/uapi/hcuapi/auddec.h`, build tree; sizeof 608→632 → AUDDEC_INIT compilado == fábrica). uImage 9l `f1e8e3eb` 7,127,844 B desplegado → **TEST FÍSICO: AUDIO FUNCIONA** en video_player (MP3, `GET_CUR_TIME` ×1442 = playback sondeando) y en juegos (sound_driver_init vivo durante todo el juego; virtuart AVP: `open /dev/sndC0i2so CFG 48K bitdepth 16`). Evidencia: `docs/experiments/evidence-9l-*.log`.
- **Iteración 9m (Fase 8/media — FIX VIDEO: mismo patrón, drift ABI 20 bytes en video_config; DESPLEGADO, test físico pendiente):** mismo método, aplicado al path de video: scan de constantes ioctl reconstruidos de pares `lui+ori` en los binarios de fábrica `libffplayer.so`/`libhudi.so` (método VALIDADO: extrae exactamente `AUDDEC_INIT=0x82780301` observado on-device) → **fábrica envía `VIDDEC_INIT=0x82980400` (sizeof 664) vs SDK compilado `0x82840400` (sizeof 644) → drift 20 bytes**. Mecanismo idéntico al audio: sin match del case, `KSHM_WRITE_HDL_ACCESS` no fluye → el AVP decodifica (virtuart: `VER:H264 allocsz=0x321b000 vdec sync stc`) pero sin acceso a buffers de frames → video sin imagen. **FIX: `_pad_abi_2025[20]` al final de `struct video_config`** (`include/uapi/hcuapi/vidmp.h`) → sizeof 664, verificado por probe mips32r2 (`VIDDEC_INIT=0x82980400`) y objdump (`lui v0,0x8298` compilado en avp-proxy.o junto al `0x8278` del audio). BUILD PASS: uImage **`1b095ac8` 7,127,573 B** (0x80000000/0x803db8a0). **DEPLOY a SD autorizado y verificado read-back** (stock.bak `53b3e0b3`/avp `a9788995` intactos). Parches propios capturados: `patches/kernel/0001` (auddec 24B), `0002` (vidmp 20B), `0003` (amprpc debug), `0004` (avp-proxy debug) — el repo ahora refleja TODO el delta kernel-side vs SDK pristino. ADR-012 (política de drift ABI fábrica↔SDK). **TEST FÍSICO PENDIENTE: reproducir un video → imagen visible** (+ regresión: audio/juegos siguen OK). Docs: `docs/experiments/2026-09-18_fase9m-video-abi-fix.md`.

## 2026-09-16

- **Iteración 6w (Fase 5 — BUILD MENU-GOAL stock-parity):** forense CORRUPT-header cerrado (archivo fabricado para test de kernel inválido; orientación descartada por el usuario — NO corrupción de SD). Auditoría binaria stock↔nuestro: cmdline idéntica vía DTB; initramfs STOCK extraído/validado (cpio CRUDO de 380 archivos, 3,849,052 B, @4,857,000 del vmlinux fábrica — corrige claim 6s). **Dos hallazgos críticos: (1) S41hcdaemon/S99app eran no-ops desde 6h → ningún test físico previo pudo llegar al menú; (2) nuestro initramfs embebido salía COMPRIMIDO (gzip, luego LZ4, vía CONFIG_RD_*) mientras la fábrica embebe CPIO CRUDO.** Cambios: overlay restaurado == stock (S41/S99 reales desde .orig, S11diag eliminado); fragment RD_* off (cpio CRUDO embebido, paridad fábrica); build_kernel.sh cpio -R 0:0. **BUILD PASS**: uImage `fc501a83...` 4,334,171 B (Load 0x80000000/Entry 0x803DE0F0), initramfs embebido CRUDO 3,849,216 B verificado == stock (diff -r vacío, S41=`hcdaemon&` real), gates TOOLCHAIN+PATCH+DTB PASS. Staging `D:\R36SX\staging\vmlinux.uImage-r36sx-v26-menu-6w`. DEPLOY+TEST FÍSICO PENDIENTE (objetivo: MENÚ TreeFrogUI). Docs: docs/experiments/2026-09-16_menu-goal-stock-parity.md.

- **Iteración 6x (Fase 5 — TEST 6w RESULTADO + drivers NOEXTRA reactivados):** boot físico del 6w (`fc501a83`): consola enciende → logo TreeFrogUI → **icono de batería arriba a la derecha (capa UI visible por PRIMERA vez en todos los tests)** → se queda ahí SIN menú. Kernel íntegro post-boot (hash re-verificado); cero escrituras nuevas en SD (inconcluso: el stock tampoco escribe en boot normal). Interpretación: cadena Linux→initramfs→SD→icube parcialmente viva; hang en la capa del menú (rkgame→zhijack→picoarch). **La refutación NO-GE de 6r queda INVALIDADA** (testeada con S41/S99 no-op — el menú no podía arrancar por diseño). Acción: retirar TODAS las desactivaciones NOEXTRA (GE/WDT/IRC/HWSPINLOCK/LVDS/NAND/TOE/I2C) — el menú requiere /dev/ge (ABI driver_r36sx.so) y hwspinlock (AMPRPC AVP) → config = vendor baseline + CHECK_ADC. **BUILD PASS**: uImage `017adf3b...` 4,348,576 B (0x80000000/0x803E3AC0, hcrc+dcrc VERIFICADOS), initramfs CRUDO == stock, gates PASS. Staging `vmlinux.uImage-r36sx-v26-menu-6x`. DEPLOY+TEST PENDIENTE. Docs: docs/experiments/2026-09-16_menu-goal-stock-parity.md (addendum).

- **Iteración 6x-RESULT (Fase 5 — PHYSICAL PASS ✅):** boot físico del 6x (`017adf3b`): **la consola llega al MENÚ PRINCIPAL de TreeFrogUI, navegable y funcional** — primera vez con kernel propio en la historia del proyecto. Verificación post-boot: vmlinux.uImage en SD == `017adf3b` íntegro (el dispositivo booteó NUESTRO kernel), stock.bak/dtb/avp stock intactos; cadena de procedencia build==staging==SD; ep `0x803E3AC0` distintivo. CAUSA RAÍZ de todos los fallos previos confirmada triple: (1) sin initramfs embebido hasta 6f; (2) S41/S99 no-ops 6h–6v (menú amputado por diseño); (3) drivers NOEXTRA off rompían la capa del menú (/dev/ge + hwspinlock). **FASE 5 COMPLETA — PHYSICAL PASS.** Estado del sistema: kernel 100% nuestro; initramfs = contenido stock (deliberado); DTB/AVP/bootloader stock intactos. Siguiente: Fase 6 (contrato TreeFrogUI sobre kernel propio); opcional: captura on-device de /proc/version como evidencia definitiva. Docs: docs/experiments/2026-09-16_menu-goal-stock-parity.md.

- **Iteración 7a (Fase 6 — INICIO contrato TreeFrogUI + captura on-device):** creado `scripts/diagnose_console_6x.sh` (deployado a `G:\diag6x.sh`, sha256 en staging de la SD): captura con NUESTRO kernel corriendo — A) `/proc/version` (evidencia definitiva Fase 5: `dafunknoise@DFNK`), B) ABI `/dev` por nodo (matriz 6m), C) **fds `/dev` abiertos por proceso** (dependencias reales vivas de icube/rkgame/picoarch/hcdaemon), D) maps de librerías, E) mounts/input/fb/ps, F) dmesg del kernel propio. `TREEFROGUI_COMPATIBILITY.md`: matriz pre-llenada con evidencia estática — VALIDADO (6x diferencial): /dev/ge, hwspinlock, S41/S99, mounts SD; PROBABLE (a confirmar con DIAG6X): dis, audio, mmz, persistentmem, standby, backlight. **PENDIENTE: usuario ejecuta `sh /mnt/sdcard/diag6x.sh` en FrogShell (kernel 6x) y trae la SD.**

- **Iteración 7b (Fase 6 — análisis DIAG6X + hallazgo bind /etc):** captura on-device ANALIZADA: `/proc/version` = **`dafunknoise@DFNK #18`** (evidencia definitiva Fase 5). ABI viva del contrato: picoarch→{auddec,check_adc1,check_adc5,fb0,ge,mem}; hcdaemon→**ZZd2C** (existe en SDK — 6l era incorrecta); cubevol→{check_adc1/5,dis,fb1,hdmi,mem}; input = pipeline **cubevol gpio→/tmp/joy_key** (sin evdev, igual que fábrica). /dev nuestro ⊇ fábrica. dmesg propio: mmc SDXC OK, GE init OK. **HALLAZGO CRÍTICO: el bind `/etc` de S99app NO ocurre bajo nuestro kernel** (fábrica 7 binds, nosotros 6; S99app byte-idéntico, rootfs/etc presente) — candidato a causa de videos rotos + salida-FrogShell. Test causal `diag7.sh` desplegado a G:\ (bind manual + errno + probar video/frogshell con bind activo). Docs: docs/experiments/2026-09-17_fase6-diag6x-analysis.md.

- **Iteración 7c (Fase 6 — síntomas refinados + BUILD lean-fábrica):** tests físicos con 6x: **audio muerto en TODO** (juegos/música/video), **video sin imagen** ("parece reproducir", nada visible/audible), **salida de cores colgada → pantalla crema** (FrogShell y emuladores no vuelven al menú); juegos y menú funcionan (fb0/GE Linux puro OK). diag7 ×2: **bind /etc funciona en boot** (ausencia en diag6x = one-off) + manual rc=0 → **hipótesis /etc REFUTADA**. Diff dmesg fábrica↔nuestro: `[decrypt_sector_data]` ×3 SOLO fábrica (hook propietario); extras nuestros: i2c-dev (HC_I2C), IR NEC (HC_IRC), ubi/watchdog, squashfs, bridge (TOE), `unable to open an initial console` (devtmpfs esconde /dev/console), `devpts bogus options`. **Hipótesis: HC_I2C interfiere con el bus del códec de audio del AVP.** BUILD 7c: REMOVE HC_I2C/HC_IRC/HC_WDT/HC_NAND/HC_TOE/HC_LVDS, KEEP HC_GE+HC_HWSPINLOCK (probados requeridos). uImage `15ad1cb7...` 4,338,380 B (0x80000000/0x803DF5C0), initramfs CRUDO == stock, hcrc+dcrc VALIDOS. **STAGED NO desplegado** — primera acción próxima sesión: deploy + test. diag8.sh en G:\ (interrupts/iomem/fds). Docs: 2026-09-17_fase6-diag6x-analysis.md (addendum).

- **Iteración 7d (Fase 6 — 7c REFUTADA + comparativa interrupts):** deploy 7c verificado (`15ad1cb7` en G:, read-back ×2) → boot OK al menú → **síntomas IDÉNTICOS a 6x** (juegos sin sonido + salida colgada crema; música/video mudos; FrogShell colgado en amarillo). **Hipótesis I2C REFUTADA — los drivers extras no son la causa.** Diferencias restantes fábrica↔nuestro: hook `[decrypt_sector_data]` (solo fábrica), warning initial-console (devtmpfs vs /dev estático), devpts bogus, stack TCP/squashfs, tamaño kernel (posible raza AMPRPC de timing). Plan 7d: diag8 v2 (snapshot doble /proc/interrupts + /proc/devices + misc + iomem + mmz + sysfs + fds) bajo 7c y bajo stock.bak → diff de IRQs → localizar el servicio AVP muerto. Vía definitiva: serial ADR-011. Docs: 2026-09-17_fase6-diag6x-analysis.md.

- **Iteración 7e-RESULT (Fase 6 — UART refutado + revisión estratégica):** deploy 7e (`da76d6ea`, of_serial 0 símbolos) → síntomas IDÉNTICOS → **hipótesis of_serial/UART REFUTADA**. Hipótesis restante (fuerte): **mismatch de versión media-proxy (SDK Jul-2024) ↔ firmware AVP (fábrica Dic-2025)** — la fábrica incluye código ausente del SDK (decrypt_sector_data, ttyHC) y media es la única familia dependiente del ABI proxy-AVP; AMPRPC sano a la misma tasa (~300 IRQ/s) en ambos kernels. **Decisión del usuario: el fix de media NO bloquea el objetivo del proyecto** — Fase 6 contrato esencialmente DONE (matriz viva + limitación conocida documentada); Fases 7-8 no bloqueadas para desarrollo. Vías al retomar: USB-TTL (ADR-011), diff binario avp-proxy fábrica-vs-nuestro, SDK más nuevo (improbable). Docs: 2026-09-17_fase6-diag6x-analysis.md (estado de fases).

- **Iteración 8-prep (cierre Fase 6 + pivot estratégico):** decisión del usuario tras revisión del objetivo: **media fix DIFERIDO** (limitación conocida; hipótesis mismatch media-proxy SDK-Jul-2024 vs AVP-fábrica-Dic-2025; 3 refutaciones limpias acumuladas: /etc bind, drivers extras, of_serial/UART). **Consola restaurada a STOCK** (verificada `53b3e0b3`) para uso diario 100% funcional; kernels propios conservados en staging (7e `da76d6ea`, 6x `017adf3b`). **Fase 6 CERRADA**: contrato TreeFrogUI validado físicamente (matriz viva diag6x/diag8; fds idénticos stock↔propio; media = única familia proxy-AVP) — ROADMAP marcado DONE. **Fase 8 (rootfs propio Buildroot) EN ARRANQUE**: primer incremento = userland 100% propio (busybox+init scripts nuestros) manteniendo el contrato de arranque al menú. Cable USB-TTL recomendado en paralelo (ADR-011 — diagnóstico de media y debugging futuro).

- **Iteración 8a/8b (Fase 8 — ROOTFS 100% PROPIO: PHYSICAL PASS ✅):** pipeline Buildroot propio: overlay mínimo (`rootfs-overlay-own`: linuxrc→busybox, inittab, rcS/S10mdev/S41hcdaemon/S99app, mdev/mount-helper, hcdaemon fabrica como única pieza propietaria) + `BR2_TARGET_ROOTFS_CPIO=y` + fragment→`rootfs-own.cpio` + build_kernel.sh flujo rootfs-cpio→linux-rebuild. 8a falló (logo eterno): **`/mnt/sdcard` y `/hcdemo_files` ausentes del esqueleto buildroot** → bind fallaba → icube.sh nunca lanzaba. 8b los crea + **S09trace** (trazador de boot a SD). **8b PHYSICAL PASS: menú TreeFrogUI navegable con kernel propio + userland propio** (busybox NUESTRO sha 691d9b24, 498 archivos, sin árbol de fábrica). Evidencia viva: `evidence-fase8-boottrace.log` (cadena init→...→picoarch completa + 7 binds). Verificado "sin resabios" (petición usuario): solo hcdaemon propietario documentado; DTB/AVP stock por diseño (ADR-008). Limitaciones: media AVP (diferida), dieta 27 MiB pendiente, S09trace self-limiting pendiente. Docs: 2026-09-17_fase8-own-rootfs.md.

- **Iteración 8c-RESULT (Fase 8 — dieta + tracer: PHYSICAL PASS ✅):** dieta del defconfig (26 paquetes vendor: FFMPEG/OpenSSL/WiFi-RTL/wpa/hostapd/libnl/hccast/prebuilts/ntfs/exfat + **HUDI con `default y` oculto del vendor** revelado por el dependency-chain check + LIBLVGL + HCFOTA): **rootfs 26,9→10,4 MiB, uImage 13,35→6,80 MiB** (`2081f406`). S09trace auto-limitante VERIFICADO (muere tras picoarch; log 7,995 B). **Boot físico: menú navegable**. Timeline (evidence-fase8c-boottrace.log): init@1,08s → SD+icube@5,53s → **menú@8,38s** — la cadena icube→rkgame→hijack consume ~2,9s → **el 8d (zhijack-direct) es también la optimización de arranque**. Docs: 2026-09-17_fase8-own-rootfs.md (addendum 8c).

- **Iteración 8d-RESULT (Fase 8 — icube-direct: PHYSICAL PASS ✅ — cadena de fábrica ELIMINADA):** S99app-direct (espera zhijack.sh + env LD_LIBRARY_PATH + lanza zhijack directamente). **Prueba definitiva: `icube` renombrado a `icube.disabled` en la SD y la consola bootea al menú navegable igual** — cero dependencia del launcher de fábrica (icube/rkgame/libemu_tfhijack fuera del boot path). uImage `03807054`. Evidencia evidence-fase8d-boottrace.log: ps limpio (zhijack→cubevol→nosleep→picoarch+frogui). Timeline: menú@~8,1s — el launcher solo costaba ~0,3s; **el pozo real es la fase de montaje SD (1,1→5,5s: mdev+mount-helper sleeps+binds)** → próxima optimización. Docs: 2026-09-17_fase8-own-rootfs.md (addendum 8d).

- **Iteración 8f-RESULT (Fase 8 — boot-opt: sin ganancia; pivot a AVP-media):** S99app montaje directo + poll 0.1s + mount-helper sleep 1→0.2 (`d104f4d7`). Test: boot sigue ~8s — el floor dominante es el init de TreeFrogUI (~4-5s, lado frogui, no nuestro). Usuario acepta ~8s. **PIVOT de prioridad: fix de raíz del problema AVP-media** (audio juegos/música/video + salida de cores) como requisito para declarar Fase 8 COMPLETA. Estrategias: (A) USB-TTL serial ADR-011, (B) sonda RPC on-device diag9, (C) diff binario avp-proxy fábrica-vs-SDK, (D) SDK más nuevo vía ecosistema. Docs: 2026-09-17_fase8-own-rootfs.md (addendum 8f).

- **Iteración 8g (Fase 8 — PIVOT AVP-media: análisis + estrategias):** diagnóstico consolidado: transporte AMPRPC SANO (misma tasa IRQ que fábrica, ZZd2C funciona, **dmesg sin un solo error del proxy** — el AVP ACKa en silencio), muerto SOLO la familia media + drain de cores. **Evidencia nueva: avp.uImage de fábrica = build "avp-custom" de la MISMA línea hclinux del SDK** (strings: E3100_R36/build/avp-custom, FreeRTOS/MIPS32_HC16xx) → drift de versión Jul-2024↔Dic-2025, ABI parcheable. Estrategias: (A) USB-TTL usuario pendiente, (B) **diag9 DESPLEGADO** (consola del AVP vía virtuart + open-probe + dmesg), (C) diff binario proxy, (D) SDK vía comunidad, (E) I2S último recurso. Checklist Fase 8 completa definida (audio/música/video/salida-cores = 1 causa raíz). Docs: 2026-09-17_fase8-own-rootfs.md (addendum 8g).

- **Iteración 8g-final (Fase 8 — diag9 comparativa: ABI opens IDENTICO):** ronda-2 stock (evidence-diag9-stock.log): opens idénticos (media rc=0; los MISMOS 4 rechazos sndC0spo/i2si/vindvp/pq → normales del hardware, no nuestro bug); virtuart mudo en ambos. **CONCLUSIÓN: el drift está 100% en los ioctls post-open (interpose kshm-handles/HW-info del proxy — código versión-dependiente Jul-2024 vs Dic-2025).** Diagnóstico empírico agotado sin serial. Estrategia C = RE del proxy de fábrica (localizable por pr_err strings idénticos en stock.bin) + diff dispatch de ioctls + parche. SD de vuelta en 8f (d104f4d7). Incidente stock-icube documentado (protocolo: stock necesita cubegm/icube). Docs: 2026-09-17_fase8-own-rootfs.md (ronda diag9-2).


- **Iteración 9a (Fase 9 — BUILD PROPIO DEL AVP: bloqueador ADR-008 CAE):** decisión del usuario tras análisis: reemplazar el AVP de fábrica por uno NUESTRO compilado del SDK (par proxy+AVP consistente Jul-2024+Jul-2024) en vez de RE del ABI. **Toolchain bare-metal CONSEGUIDO** (Codescape 2019.09-03-2 MIPS32 MTI, 364MB, Google Drive del ecosistema tzubertowski) — el "toolchain privado" de ADR-008 era público/archivado. Build standalone (defconfig canónico + CROSS propio + DTS propio + entry.ld canónico creado — faltaba del SDK). **avp.bin propio 2,3MB; strings unicos IDENTICOS a fábrica (8,143=8,143 — misma fuente); uImage load/ep 0x8BDA4000 == fábrica, hcrc válido.** Deploy+test pendiente. Docs: 2026-09-17_fase8-own-rootfs.md (Fase 9a). AVP uImage sha256: 

- **Iteración 9f/9g-RESULT (Proxy-RE: EL BUG ESTÁ EN USERSPACE):** kernel con debug amprpc (500 entradas + snd_xfer logging + dmesg completo). 114 llamadas RPC: TODAS exitosas, CERO timeouts — **el AMPRPC y el AVP funcionan perfectamente**. PERO: **picoarch NUNCA llama AUDDEC_INIT, SND_IOCTL_HW_PARAMS, GET_HW_INFO, START, ni SND_IOCTL_XFER** — snd_xfer nunca se ejecuta. Solo cubevol gestiona volumen (SET/GET_VOLUME, SET_HDMI_MUTE). **El fallo NO está en kernel/AVP — está en USERSPACE: picoarch/driver_r36sx.so falla silenciosamente antes de tocar el kernel.** Debug de TreeFrogUI activado (log.txt en SD) para ver el error. Docs: evidence-9g-boottrace.log + experimento 9f/9g.

- **Iteración 9h (Proxy-RE): treefrog_ui.log capturado — audio driver SÍ inicializa pero ABORTA silenciosamente.** S09trace ahora copia /tmp/treefrog_ui.log a SD. evidence-9h-treefrog-ui.log: sound_driver_init abre /dev/auddec (fd OK), envía AUDDEC_START, recibe buffer_size=655359 y volumen=100 del AVP — **pero inmediatamente llama sound_driver_deinit**. Sin mensaje de error explícito. **El AVP responde correctamente — es driver_r36sx.so quien aborta.** Siguiente: analizar el boottrace COMPLETO para ver el retorno de AUDDEC_INIT (el cmd de formato que probablemente falla y dispara el abort). Docs: experimento 9h.

## 2026-09-15

- **Iteración 6 (Fase 5 — inicio, SAFE PHYSICAL BOOT TEST):** autorizado por el usuario. PASO 0 re-sync completado (HEAD 528e51b == remote, worktree limpio, preflight PASS 24/24, SD stock G: verificada: vmlinux.uImage `53b3e0b3...`, dtb `1258f1eb...`). PASO 1 backup: **VÍA B (filesystem-level)** — usbipd-win attach de la SD (`/dev/sde`, lector 05e3:0723) OK; se descartó `dd` completo por latencia USB (~55 min) y se eligió `tar` de solo archivos usados → `~/backups/r36sx-sd-files-20260915.tar.gz` (415,381,115 B, sha256 `97086531ea...`, 2474 entradas cubegm/, vmlinux stock `53b3e0b3...` dentro verificada). Limitación Vía B documentada (no bit-a-bit; válido por no reparticionar). SD devuelta a Windows como G: (unbind, verificada intacta). **Nota cuenta:** remote real = `ozkaoz`/`Odkwr` r36sx-hclinux (el prompt refería `Dafoker` — misma cuenta, no bloqueante).
- **Iteración 6b (Fase 5 — PASO 2+3 + BOOT PARCIAL):** PASO 2 artefactos verificados 7/7 OK (`vmlinux.uImage` `9821559d...` para desplegar; **dtb.bin NO se toca** — el de SD `1258f1eb...` ya es correcto). PASO 3 staging: `D:\R36SX\staging\vmlinux.uImage-r36sx-v26-fase4b` (hash tras copia `9821559d...` OK). **Swap del usuario en G: ejecutado y verificado** (`9821559D...`, backup `.stock.bak` creado). **BOOT FÍSICO PARCIAL:** consola enciende, muestra splash TreeFrogUI (fb0 init, criterio b PASS) pero **NO llega al menú ni navegable (criterio c FAIL)**. → **PASO 5 (rollback) EJECUTADO Y VERIFICADO:** restaurado stock `53b3e0b3...`, consola vuelve a arrancar TreeFrogUI normal (ROLLBACK OK). Diagnóstico: el `.config` de fábrica no es extraíble (sin IKCONFIG, no ELF); confirmado que el entry del uImage es respetado por bootm (no es la causa). Experimento: docs/experiments/2026-09-15_fase5-physical-boot.md.
- **Iteración 6c (Fase 5 — DIAGNÓSTICO C):** hipótesis fuerte de causa raíz. La UI (driver_r36sx.so/icube/MyExecutable) abre `/dev/check_adc1` (batería) y `/dev/check_adc5` (charging); el DTS stock declara nodos `check_adc0..5@18818400`; pero **`CONFIG_CHECK_ADC is not set`** en el config vendor (igual en baseline d3100) → `hc_check_adc.c` no se compila → no se crean `/dev/check_adc*` → la UI puede quedarse en splash (explica boot parcial). Acción: habilitar `CONFIG_CHECK_ADC=y` y recompilar. **ADR-011** (diagnóstico serial: DTB serial-only NO-baseline para dmesg vía USB-TTL, hc_uart@18818600 115200n8; cable USB-C OTG NO sirve — solo gadget MTP/PTP). **`scripts/diagnose_boot_serial.sh`** genera el DTB de diagnóstico (33109 B, `7278effe...`). Rollback OK y consola usable.
- **Iteración 6d (Fase 5 — RE-BUILD CONFIG_CHECK_ADC=y):** kernel r36sx-v26 recompilado con `CONFIG_CHECK_ADC=y` vía fragmento `boards/r36sx-v26/kernel/r36sx-v26.config.fragment` + `BR2_LINUX_KERNEL_CONFIG_FRAGMENT_FILES` (vendor config base intacto). Gates PASS (TOOLCHAIN/PATCH/DTB SEMANTIC 0 diff). `.config`: `CHECK_ADC=y`. **Nuevo vmlinux.uImage `08cced35...`** (Load 0x80000000, Entry 0x803E3AA0, gzip). dtb.bin `04fb8383...` sin cambios. Artefactos 7/7 OK; staging `D:\R36SX\staging\vmlinux.uImage-r36sx-v26-fase4b-checkadc`.
- **Iteración 6e (Fase 5 — RE-TEST FALLÓ, hipótesis C REFUTADA):** el boot con `CONFIG_CHECK_ADC=y` (`08cced35...`) sigue quedándose en splash sin menú. **`CONFIG_CHECK_ADC` NO era la causa única.** Hallazgo adicional: `/dev/backlight` requerido por UI pero `CONFIG_FB_BACKLIGHT is not set` en vendor. **NO hay logs de boot en la SD** (menu.log binario no se reescribe). DECISIÓN: NO recompilar más por conjetura — se requiere **dmesg via serial (ADR-011)** para diagnóstico no-ciego. **ROLLBACK a stock `53b3e0b3...` ejecutado y verificado (consola usable).**
- **Iteración 6f (Fase 5 — CAUSA RAÍZ ENCONTRADA + RE-BUILD INITRAMFS):** análisis del uImage stock reveló que **el kernel de fábrica embebe su rootfs como initramfs dentro del vmlinux** (cpio `070701`, `etc/init.d/rcS`, `usr/bin/hcdaemon` — offsets 5097761+). Nuestro build NO lo hacía (`BR2_TARGET_ROOTFS_INITRAMFS not set`, `CONFIG_BLK_DEV_INITRD not set`) → con `root=/dev/ram0` el kernel no encontraba root → boot parcial/pantalla negra. Explica por qué S50diag/S01diag en la SD no se ejecutaron (el sistema usa el initramfs embebido, no los scripts de la SD). **Solución aplicada:** `BR2_TARGET_ROOTFS_INITRAMFS=y` (+CPIO, squashfs off) + `CONFIG_BLK_DEV_INITRD=y`/RD_* + `CONFIG_INITRAMFS_SOURCE=rootfs.cpio`. **Re-build PASS**: vmlinux 78MB con initramfs, **uImage `004b2d50...` 13.7MB** (antes 2.7MB), gates TOOLCHAIN/PATCH/DTB SEMANTIC PASS. Staging `D:\R36SX\staging\vmlinux.uImage-r36sx-v26-initramfs`. RE-TEST físico pendiente.
- **Iteración 6g (Fase 5 — RE-TEST initramfs llegó a splash + RE-BUILD S99app):** RE-TEST del kernel initramfs (`004b2d50...`) → llegó al **splash TreeFrogUI pero NO al menú** (avance vs pantalla negra). Causa: faltaba **`etc/init.d/S99app`** (script del initramfs stock que monta /mnt/sdcard vía bind + activa swap + lanza `icube.sh`). **Añadido S99app al rootfs-overlay** de la board + `BR2_ROOTFS_OVERLAY` configurado + `build_kernel.sh` copia overlay. **Re-build PASS**: uImage **`0ddcd33a...`** 13,674,373 B (initramfs + S99app), gates PASS. Staging `D:\R36SX\staging\vmlinux.uImage-r36sx-v26-initramfs-s99app`. RE-TEST físico pendiente.
- **Iteración 6h (Fase 5 — INITRAMFS DEL DESARROLLADOR como rootfs):** RE-TEST del kernel con S99app (`0ddcd33a...`) → **siguió en splash sin menú**. Causa: el rootfs Buildroot embebido usaba un `hcdaemon` **stub** (6,212 B), no el daemon real del desarrollador (610,404 B). **Solución:** extraído el initramfs stock completo (3,852,472 B, 380 archivos) como rootfs de la board; `CONFIG_INITRAMFS_SOURCE` → `rootfs-dev.cpio` (3,849,216 B con hcdaemon real/libc/busybox/S99app); `BR2_TARGET_ROOTFS_INITRAMFS` desactivado. **Re-build PASS**: uImage **`707fcec8...`** 4,352,104 B (initramfs minimal del desarrollador vs 13.7MB Buildroot), gates PASS. Staging `D:\R36SX\staging\vmlinux.uImage-r36sx-v26-devrootfs`. RE-TEST físico pendiente.
- **Iteración 6i (Fase 5 — S11diag FIX + re-build dmesg diagnóstico):** al preparar el re-test físico se detectó un **bug en S11diag**: corre en S11 (ANTES de S99app), cuando ni `/mnt/sdcard` ni `/media/*/cubegm` existen aún (la SD se monta asíncronamente vía mdev; S99app hace el bind a `/mnt/sdcard`). El `LOG=/media/*/cubegm/dmesg_boot.log` usaba un **glob literal (no expandido)** → el redirect fallaba → no se capturaría dmesg → test físico desperdiciado. **S11diag reescrito** con wait-loop (hasta 20s) que espera a que mdev monte la SD en `/media/*/cubegm` antes de escribir `dmesg_boot.log`. **Re-build PASS**: uImage **`0fef5fd1...`** 4,354,188 B (initramfs dev + S11diag fix, hcdaemon real 610KB, S99app), gates TOOLCHAIN/PATCH/DTB SEMANTIC PASS, kernel.config `793a3ab7` (BLK_DEV_INITRD/CHECK_ADC/INITRAMFS_SOURCE=rootfs-dev.cpio). **Desplegado en G: `0fef5fd1...`** (stock.bak `53b3e0b3` intacto, dtb/avp sin tocar). RE-TEST FÍSICO PENDIENTE: boot + leer `G:\cubegm\dmesg_boot.log`.
- **Iteración 6j (Fase 5 — RE-TEST FÍSICO #1 + S11diag v2):** boot del kernel `0fef5fd1` (S11diag v1) → **NO se generó `dmesg_boot.log`** (1ª vez pantalla negra, reboot → splash TreeFrogUI sin menú). Análisis del mecanismo de montaje (mdev.conf → `/etc/mdev/mount-helper.sh` monta SD en `/media`; S99app espera `/media/*/cubegm/icube`): el fallo en S11diag Y en S99app implica que **la SD NO se monta en runtime** → inferencia fuerte: **nuestro kernel mmc (MMC_DW/HC_SDIO) no detecta/inicializa la tarjeta** como el de fábrica (config sí tiene MMC/VFAT; el problema es runtime). **S11diag v2** reescrito: captura dmesg a /tmp siempre + **monta manualmente la SD** (mmcblk0p1) si mdev falla + vuelca estado mmc/sysfs/input/fb/dtb. **Re-build PASS**: uImage **`6d44c1b7...`** 4,354,009 B, S11diag v2 verificado embebido (MATCH). **Desplegado en G: `6d44c1b7...`** (stock.bak/dtb/avp intactos). RE-TEST FÍSICO #2 PENDIENTE: boot + leer `G:\cubegm\dmesg_boot.log` para confirmar si el kernel mmc detecta la SD y obtener el dmesg.

- **Iteración 6r (Fase 5 — VARIANTE NO-GE REFUTADA + cierre sesión):** test variante NO-GE (`cd37a60f`, CONFIG_HC_GE=n): el usuario vio **'please insert tf card' desde el inicio** (AVP esperando SD), sin parpadeo ni `dmesg_boot.log` en la SD. **GE NO era la causa** (o no la única). El kernel sigue sin llegar a userspace (o la pantalla está 100% controlada por el AVP, invisible para Linux). **ROLLBACK a stock `53b3e0b3` ejecutado y verificado** (consola usable, dtb/avp intactos). **Conclusión:** sin serial no se puede localizar dónde se cuelga el kernel (todos los S11diag sin log ni beacon; 'insert tf card' es del AVP). Próximo: serial ADR-011 (necesita cable USB-TTL) o seguir con variantes de init de dispositivos.
- **Iteración 6s (Fase 5 — COMPARACIÓN INITRAMFS STOCK + cubegm):** extraído el initramfs del vmlinux STOCK (cpio CRUDO, 50 archivos); nuestro rootfs-dev.cpio (53) tiene TODOS los stock + 3 de diagnóstico; scripts de montaje (S10mdev/S99app/mdev.conf/mount-helper.sh/inittab/linuxrc/rcS) IDÉNTICOS. **cubegm NO monta el sistema** (scripts asumen /mnt/sdcard montado; la SD tiene rootfs/ 313MB bind-montado por S99app). **CONCLUSIÓN: el montaje lo hace el initramfs (S99app), correcto e idéntico al stock; la SD se montaría si el kernel detectara mmcblk0p1. El bloqueador es 100% del kernel (mmc runtime).** Se descartan hipótesis de initramfs incompleto y cubegm-montaje. Docs: docs/experiments/2026-09-16_initramfs-stock-comparison.md.
- **Iteración 6v (Fase 5 — VARIANTES SIN EFECTO + uImage/clk verificados):** la variante NO-EXTRA (GE/watchdog/irc/lvds/nand/toe/i2c/hwspinlock desactivados) se aplicó y desplegó correctamente (verificado en vmlinux) pero el boot NO cambia ('insert tf card'). uImage header válido (magic/os/arch/type/comp/load idénticos al stock). cpu_clk SÍ se registra (clk_base.c via 'hichip,base-clk'), no hay panic en plat_time_init. **Pregunta abierta: ¿nuestro kernel siquiera carga?** Todo lo visible podria ser del AVP (pantalla controlada por AVP, config no observable). Próximo test decisivo sin serial: desplegar un kernel deliberadamente invalido para ver si el comportamiento cambia.
## 2026-09-14

- **Iteración 1 (Fase 0):** bootstrap desde cero. Preflight WSL PASS; inventario STATIC PASS (5 fuentes top-level + hashes → manifests); repo local + estructura completa; AGENTS/CURRENT/CONTEXT_MAP/DECISIONS (ADR-001..006); contratos docs/ai/*; agentes .opencode; scripts core; repo GitHub creado + push inicial. Repo previo homónimo archivado a bundle y eliminado por decisión del usuario (ADR-003).
- **Iteración 2 (Fase 1):** SDK extraído y auditado (6 pasadas + addendum). H1–H11 CONFIRMED con evidencia (Buildroot 2021.05-rc2, arch HC16xx vía rsync linux-drivers, 4.4.186 default/5.12.4 secundario, AMPRPC/AVP, boot chain con direcciones 0x85ff0000/0xb8800004/0x8BDA4000, toolchains Codescape). Patches /mnt/d ≡ SDK (hash idéntico). Cross-check manual↔SDK: 7 CONFIRMED + 1 PARTIAL (DDR-init 12 KiB ≠ 4 KiB). Matching físico R36SX→D3100 v20 integrado (evidence del usuario). 2 refs .md del manual añadidas al manifest (7/7 PASS). docs/SDK_AUDIT.md + ARCHITECTURE.md + BOOT_CHAIN.md completados. STATIC PASS.
- **Iteración 2b (gobernanza):** AGENTS.md §13 DOCUMENTATION SYNC permanente (review 12 puntos pre-commit; iteración incompleta si docs impactadas sin actualizar); orchestrator bloquea cierre; documentation-engineer ejecuta review. Regla del usuario.
- **Iteración 3 (Fase 2+3):** VENDOR BASELINE KERNEL-ONLY d3100_v20 **BUILD PASS** (4 intentos; fixes mínimos documentados: HCCAST kconfig bug vendor, -fcommon GCC13, kernel-only ADR-008). Artefactos: vmlinux.uImage (0x80000000/0x803e3200), dtb.bin byte-idéntico a baseline previo, rootfs.squashfs — persistidos en D:\R36SX\hclinux-builds\. **Validación física SD stock (G: read-only): vermagic fábrica usa MISMO toolchain (Codescape 2018.09-02) que nuestro build; DTB stock = board E3100v20 (corrige reporte previo); Linux stock 176MiB; console=tty1** → ADR-007 (d3100_v20=solo pipeline), ADR-008 (kernel-only). Fase 3 resuelta por evidencia física. HOST PASS + BUILD PASS. Sin flashear.
- **Iteración 4 (Fase 2.5 — provenance audit):** KERNEL PROVENANCE GATE permanente (AGENTS §14) + 4 agentes. **Cross-compilación PROBADA**: 1515 .cmd kbuild invocan mips-mti-linux-gnu-gcc (117 subsistemas), ELF kernel/busybox/.ko MIPS32r2 LE, Codescape 2018.09-02 triplet/sysroot identificados. **Patches PROBADOS**: output aislado + log V=1 (rsync BSP → yaffs2 → 41 Applying en orden → .stamp_patched), hunks verificados, 62/62 externos D:\ byte-idénticos (VERIFIED IDENTICAL, sin doble aplicación), conteos reconciliados (41+overlay yaffs2; "45" era impreciso). **E3100**: chipid enum SDK sin board files → BOARD_IDENTITY.md. Corregido "vermagic==toolchain" en docs. Gates reproducibles: scripts/audit_toolchain.sh + audit_kernel_patches.sh. Remoto verificado ×3. ALL GATES PASS.
- **Iteración 5 (Fase 4):** **4A stock model**: contradicción memoria RESUELTA (256 MiB = Linux 175.57 + AVP 80.43 [FBstatic/sysmem/mmz1/mmz0]; "AVP 176" era error de redacción; "SDK 254 MiB" eran macros v10 — v20 real: 128/79.20). DTB stock decompilado + roundtrip SEMANTIC PASS; panel DEMOSTRADO: MIPI-DSI lcd-dsi0-r63311 ausente en SDK; nodo /panel identidad consola inexistente en SDK; **NOR 3 particiones → consola bootea kernel desde SD**. 15 deltas formales (7 CRITICAL) reemplazan las "8" informales (2 orientaciones corregidas). **4B board r36sx-v26**: defconfig propio (8 cambios REQUIRED), DTS generado por script (stock verbatim + macros), **kernel BUILD PASS: DTB SEMÁNTICAMENTE IDÉNTICO al stock (allowlist 0, byte-idéntico al roundtrip), kernel config delta 0 vs vendor, TOOLCHAIN+PATCH PROVENANCE PASS**. ADR-010. Upstream vendor intacto. **PHYSICAL: NOT TESTED.** Fase 5 propuesta, esperando autorización.
- **Iteración 6l (Fase 5 — DIAG2 FÁBRICA via FrogShell):** SD a stock + `diag2.sh` ejecutado en FrogShell → `evidence-factory-diag2.log`. **Hallazgos:** (1) `/proc/config.gz` NO existe → la fábrica NO tiene IKCONFIG_PROC (config exacto no extraíble). (2) La fábrica usa **EL MISMO driver** `hichip,dw-mshc` (`dw_mmc_hc 1884c000.mmc`, host clk 198MHz, source clock 0x3, Version 270a, irq 10) y **detecta la tarjeta** (`mmc0: new high speed SDXC` → `mmcblk0: SD64G 59.5 GiB` → `p1`), montando la SD en `/media/mmc` + `/mnt/sdcard` (vfat, utf8). (3) Drivers custom de fábrica `decrypt_sector_data` (llamado 3×, antes del escaneo de particiones) y `/dev/ZZd2C`: **NO están en el SDK** (binarios propietarios). (4) La SD se monta con FAT plano (blkid LABEL="R36SX") → decrypt_sector_data NO bloquea el montaje. (5) `/dev` fábrica completo, mmcblk0p1 creado. **Conclusión:** nuestro kernel tiene el driver mmc correcto; el problema es runtime (probablemente colgarse en init de dispositivo, no montar la SD, o no llegar a userspace). Se requiere dmesg de NUESTRO kernel (serial ADR-011) o vía MTP/USB.
- **Iteración 6m (Fase 5 — INGENIERÍA INVERSA cubegm):** documentado `cubegm/` (TreeFrogUI). Flujo de boot: icube.sh -> icube -> rkgame -> setting.xml autorun -> libemu_tfhijack -> zhijack.sh -> picoarch+cores/frogui_libretro.so (el menú) + driver_r36sx.so. Parámetros TF_DEVICE=R36SX 640x480, TF_PRESENT=fbwrite. **ABI de /dev del driver:** auddec, backlight, check_adc1, dis, fb0, ge, input/event0, mmz, persistentmem, sndC0i2so, standby (cubierta por nuestro config). `driver.so`==`driver_r36sx.so` (58b180a2). `driver_r36sx27.so`=variante safe (stub GE, misma ABI). Binarios MIPS32 rel2 dinámicos compatibles con la libc del initramfs dev. `modules/` solo usb_f_mass_storage/usb_f_mtp (kernel monolítico). Conclusión: la UI es cubegm+picoarch+frogui; el BLOQUEADOR sigue siendo que nuestro kernel no monta la SD en runtime (prerequisito de cualquier UI). Docs: docs/experiments/2026-09-15_cubegm-reverse-engineering.md.
- **Fase 9-4 COMPLETE (2026-09-22): kernel 5.12.4 CLEAN PHYSICAL PASS.** SD purgada de kernel-4 (autorizado; preservado hash-verified en D: backup) y la consola arranca SOLO con 5.12.4: menú TreeFrogUI + audio (ABI ADR-012 validada físicamente en 5.12) + navegación (port 9101 timer + HC_INPUT) + entrada/salida de emuladores. Boot ~10s. Historia de tests: t1 congelado-en-logo (base vendor 5.12.4 sin MMC -> bucle infinito S99app), t2 pantalla negra (display init lento en 5.12, apagado temprano; boottrace probó boot completo con SD montada vía dw_mmc nuevo), t3+confirm = menú OK. cubegm = contrato NOR de 4 archivos. Evidencia: docs/experiments/evidence-9-4-k512-cleanpass-*.log. Nuevo known-good = kernel 5.12.4 (uImage 9731d6a5).
- **Fase 9-6b COMPLETE (2026-09-23): USB Mode MTP PHYSICAL PASS en kernel 5.12.4.** Windows detecta "TreeFrogUI MTP" y la transferencia de archivos verificada fisicamente. La saga (14 tests): DUAL_ROLE (el crash HOST-only), S90configfs (mountpoint vendor pattern), modulos visibles via el bind del rootfs de fabrica, kmod-28 standalone (el busybox de fabrica segfault-ea con ELFs 5.12), gadget stack BUILT-INTO (el module loader del kernel OOPSEA en resolve_symbol con cualquier .ko — workaround documentado como 9-6c pendiente), y EL BUG RAIZ FINAL: el f_mtp Android llamaba usb_os_desc_prepare_interf_dir ANTES de config_group_init_type_name — inocuo en 4.4 (array-API), FATAL en 5.12 (list-API: configfs_add_default_group hace list_add sobre grupo zerado -> NULL deref al mkdir del gadget = el kernel reboot). Decodificado via OOPS (Comm: mkdir, BadVA 0x0, epc __list_add) + System.map. Fix: orden corregido en el port 9103. Kernel fdd1d7cc.
